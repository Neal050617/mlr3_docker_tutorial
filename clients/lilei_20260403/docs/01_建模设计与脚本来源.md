# 01 建模设计与脚本来源

修订日期：2026-05-27

## 分析目标

`20260403_lilei` 项目当前正式分析使用 case-positive 方向，包含诊断、疗效预测和动态差值建模。正式结果目录为：

```text
lina_20251208/20260403_lilei/reproduce_formal_case_positive_fullrerun_20260527_0156
```

当前 Step2 中二分类 positive class 来自 `--gp` 的第 1 个组，因此正式运行必须使用：

```text
LAEC-Con
P_NRT-NP_NRT
```

不能在模型完成后通过改表头倒推 case/control 方向。旧方向结果已删除，不再作为正式结果入口。

## Positive Class

| 分支 | control/negative | case/positive | `--gp` |
|---|---|---|---|
| `0.1-0.3` | `Con` | `LAEC` | `LAEC-Con` |
| `1.0-1.6` | `NP_NRT` | `P_NRT` | `P_NRT-NP_NRT` |

## 模型分支

| 分支 | 输入类型 | 任务 |
|---|---|---|
| `0.1` | Con-LAEC 菌群 | LAEC vs Con |
| `0.2` | Con-LAEC 代谢组 | LAEC vs Con |
| `0.3` | Con-LAEC 多组学 | LAEC vs Con |
| `1.0` | 临床基线 | P_NRT vs NP_NRT |
| `1.1` | 临床 + NRT 前菌群 | P_NRT vs NP_NRT |
| `1.2` | 临床 + NRT 前代谢组 | P_NRT vs NP_NRT |
| `1.3` | 临床 + NRT 前多组学 | P_NRT vs NP_NRT |
| `1.4` | 临床 + 菌群差值 | P_NRT vs NP_NRT |
| `1.5` | 临床 + 代谢组差值 | P_NRT vs NP_NRT |
| `1.6` | 临床 + 多组学差值 | P_NRT vs NP_NRT |

## 原始数据和输入

项目专用原始数据仍保留在项目目录：

```text
1Gp01.Con-LAEC/
2GP.1.NP_NRT-P_NRT/
2GP.2.NP_RT-P_RT/
李磊-食管癌项目-多组学样本及临床信息表20251223.xlsx
代谢物鉴定定量列表_0107.xlsx
```

正式建模输入为：

```text
v0.2.2_inputs_corrected/
```

输入整理、分组和 branch map 属于项目专用逻辑；Step1/Step2/Step3 主流程属于仓库级通用逻辑。

## 脚本来源

通用建模脚本位于仓库根目录：

```text
scripts/efs_valid_plus_step1.v0.2.2.R
scripts/efs_valid_plus_step2.v0.2.2.R
scripts/efs_valid_plus_step3.v0.2.2.R
scripts/run_mlr3_shap_all.sh
scripts/summarize_mlr3_results.R
```

项目目录只保留项目专用 wrapper：

```text
lina_20251208/20260403_lilei/scripts/run_formal_analysis_corrected.sh
lina_20251208/20260403_lilei/scripts/run_shap_all_models_sensitivity.sh
```

后续项目不要从 `lina_20251208` 或 `lina_20260515` 复制 Step1/Step2/Step3 主脚本，应直接调用仓库根目录 `scripts/`。
