# lina legacy mirror

This directory preserves the old top-level `lina/` workspace after customer
analysis directories were reorganized under `clients/`.

The full legacy mirror is under:

```text
clients/lina_legacy/legacy_mirror/lina/
```

`legacy_mirror/` is ignored by Git and should be synchronized with rsync when
preserving the full old workspace across machines.
